# Save and load scenarios

Cover manual save strings, copied data, file import, pasted-string load, malformed input, versioning, complete state restoration, language restoration, and save/load round trips.

`browser-storage.spec.cjs` integration-tests the versioned storage repository against real browser `localStorage`. Full player-facing save/export/restore journeys remain Section 5 work.
