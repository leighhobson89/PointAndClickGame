# Bugs — resolved register

Every defect that has been closed, with the evidence that closed it. Open defects and risks are in [bugs.md](../bugs.md).

Rows are never deleted from this file. A defect that reopens moves back to the active register with a note, as BUG-022 did.

Severity: **P0** data loss/security/release blocker, **P1** major journey blocker, **P2** significant defect, **P3** polish/maintainability.

## Resolved

| ID | Sev | Finding | Evidence |
| --- | --- | --- | --- |
| BUG-001 | P1 | Debug Room was intentionally removed from shipped navigation/UI/content; Section 4 owns its development-only replacement. The validator rejects its reintroduction into production content. | Content validation and startup/navigation browser tests pass. |
| BUG-002 | P1 | The Map now has generated room art, polygon grid, reciprocal navigation, a stable payoff hotspot/action, five-locale copy, and canonical completion fact. | Content/unit validation passes; navigation E2E enters and returns from Map in locked/open gate states. |
| BUG-003 | P0 | **Resolved 2026-09-13 by Section 5.** All four recorded defects are closed: the player-facing path writes a versioned `{ format, schemaVersion: 2, savedAt, payload }` envelope through one migration boundary; derived state (canvas metrics, entity placement and walk-grid stamps, visual positions, background/foreground images, entity paths, inventory strip) is rebuilt after restore; the content bundle is replaced by a patch against shipped content; and local Continue, milestone checkpoints, and room-change autosave exist. Format and policy are documented in `save-format.md`. | Eleven Node tests cover the format, patching, migration, validation, and policy; `save-load` browser coverage round-trips six milestones through a full page reload, migrates a declared legacy save, transfers a manual string into a fresh session, and proves a corrupt save, an unsupported version, and a blocked `localStorage` write each leave the running session unchanged. |
| BUG-004 | P1 | `resetAllVariables()` now replaces canonical and transient state from a fresh factory. | Covered by `game-state.test.mjs` and `game-state/new-game-reset.spec.cjs`; five repeated browser starts pass. |
| BUG-005 | P2 | The active animation frame and session resize/canvas listeners now have explicit ownership and disposal. | Counts remain stable across five starts in `game-state/new-game-reset.spec.cjs`. |
| BUG-006 | P2 | Transition helpers now await completion, remove listeners, and have a zero-duration/reduced-motion completion path. | Unit sequencing and the reduced-motion opening-transition E2E pass. |
| BUG-007 | P2 | Explicit coordinate checks now preserve `0` for either axis. | Unit coverage passes for `(0,0)`, `(0,n)`, `(n,0)`, and missing values. |
| BUG-008 | P1 | Startup checks HTTP success and JSON shape, commits the bundle atomically, displays a fatal alert, and does not start gameplay on failure. | HTTP/schema unit tests and the fatal-load browser test pass. |
| BUG-010 | P1 | Commands now carry stable verb/target IDs from buttons, inventory, and canvas targets; translated copy is presentation only. | All nine-verb unit cases and the identical five-locale librarian semantic journey pass. |
| BUG-012 | P2 | New Game now awaits required image preload and the validated data bundle before starting. | The delayed-library-background E2E keeps gameplay hidden until readiness and passes. |
| BUG-019 | P2 | chapter1-world-v1 makes the 18-room runtime topology authoritative and labels the divergent diagram/names as historical. Reciprocal graph and grid coverage are validated. | Content validation and 18-room browser traversal pass. |
| BUG-020 | P2 | Market Street intentionally retains five established connections; the four-exit brief is labelled superseded and the undeclared e6 grid cell was removed. | Validator enforces exactly five exits; all five are covered by browser traversal. |
| BUG-022 | P3 | Reopened and closed again on 2026-09-13: the Section 3 resolution missed `swapBackgroundOnRoomTransition` in `game.js`, which still passed the browser-created global `canvas` to `setDynamicBackgroundWithOffset`. That call now uses `getElements().canvas`, and a stale commented-out duplicate of the line was removed. All executable canvas access is now an explicit local, parameter, or `getElements().canvas`. | Navigation, dialogue, game-state, and puzzle browser suites pass with no named-global dependency; see the 2026-09-13 Section 4 run. |
| BUG-033 | P2 | **Found and fixed 2026-09-13, introduced in Section 1.** During a room change the previous room's foreground items were drawn on top of the new room's background. Making the transition awaitable moved the commit of `currentScreenId` to after `await fadeBackToGameInTransition()`, while the background swap stayed before it, so for the whole fade the canvas showed the new room but `drawForegroundImageForCurrentScreen()` was still asked for the departed room's foreground. The has-foreground-items flag lagged too, because it was only decided inside the background image's `onload`. The room identity now commits with the background, while the overlay is still opaque; the flag is derived synchronously from the background URL; and foreground images are cached per URL instead of being reallocated every frame. | `animation-cutscenes/room-transition-foreground.spec.cjs` samples every animation frame of a real transition and asserts the displayed background, the current room, and the foreground flag always agree. Verified against the unfixed code, where it reports 23 disagreeing frames. |
| BUG-034 | P2 | **Found and fixed 2026-09-13 by Section 6.** `#buttonRow button` carried `margin-left: 82%`, so the rule silently assumed the row would only ever hold one control. Adding the Journal button pushed it completely outside the viewport, where it was visible to CSS but unclickable; Playwright reported "element is outside of the viewport" on every attempt. The offset now applies once, as `margin-left: auto` on the first control, so the group sits right-aligned for any number of buttons. | All seven `puzzles/journal-and-hints.spec.cjs` tests open the journal by clicking `#openJournal`, and failed against the unfixed rule. |
| BUG-037 | P2 | **Reported by Leigh and fixed 2026-09-14; introduced when the librarian tutorial moved onto the explicit dialogue graph.** Asking for the research room key showed no player line and jumped to "Oh hello, you're back!" in the middle of the same conversation. Three faults: the chosen option was passed to `showText` without claiming the player as speaker and without coordinates, so it was drawn at the departed speaker's position or none at all; `library.librarian.q0.keyResponse1` pointed at the phase-1 *introduction* rather than its choices, so the librarian re-greeted the player mid-exchange, which the legacy engine never did because advancing a phase mid-conversation went straight to the new options; and the phase change was never recorded, so leaving and returning replayed the whole introduction. The phase change is now a `library.askedForResearchKey` consequence, consequences run for line and choice nodes rather than only end nodes, and the conversation enters the graph at the librarian's current phase through `createDialogueState(graph, { startNodeId })`. | `e2e/dialogue/librarian-key-journey.spec.cjs` records every line handed to the renderer with its speaker and position; it proves the chosen option is spoken by the player at the player's position, that the phase-1 greeting is absent from the continued conversation, and that returning later opens on that greeting with the phase-1 choices. Verified against the unfixed code once per fault, where it reports speaker `npc1`, the greeting spoken mid-conversation, and the phase-1 choices missing on return. Two Node tests cover the same journey at the graph level. |

| BUG-014 | P1 | **Resolved 2026-09-14 by Section 7.** Canvas interactions now have a separate named DOM mirror, concise scene descriptions, live command/state announcements, focus management, keyboard verb/inventory/dialogue traversal, Escape/back, touch parity, and optional controller navigation. | `accessibility/keyboard-and-semantics.spec.cjs` covers keyboard action selection, focus restoration, and named semantic controls in all 18 rooms. The responsive touch case performs a real touch activation; full browser suite passed 69/69. |
| BUG-015 | P2 | **Resolved 2026-09-14 by Section 7.** The fixed-percentage HUD and Bootstrap presentation were replaced by a tokenised responsive shell around a stable 832×448 logical stage, with reflowing controls and scroll-safe zoom/text expansion. | `rendering-layout/responsive-interface.spec.cjs` covers 1280×720, 1440×900, 1920×1080, 834×1112 touch, 200% text sizing, long localisation, high contrast, and reduced motion. |
| BUG-029 | P2 | **Resolved 2026-09-14 by Section 7.** The eight narrow authored exits now project to independent centred semantic targets at least 3×3 cells/44 pixels while the walk grid and authored exit bounds remain unchanged. | Unit coverage proves semantic expansion does not mutate the grid; `npm run report:hotspots` reports zero minimum-size warnings; all-room semantic and navigation browser tests pass. |
| BUG-038 | P3 | **Resolved 2026-09-14 by Section 7.** Graph dialogue now reserves three ordinary choices plus the persistent exit and uses the existing arrows to reach the remaining choices. No choice text, link, condition, action, or consequence changed. | `dialogue/scenario-dialogue.spec.cjs` asserts four readable rows, arrow navigation to the seventh option, and a continuously reachable exit; the full dialogue and browser suites pass. |
| BUG-040 | P3 | **Resolved 2026-09-19.** All four directions now have a readable nine-pose step. Front/back sources are reordered and selectively mirrored into alternating contacts; the side cycle closes into a real frame-5 knee/ankle crossover instead of retaining a wide V throughout. | `scripts/player-gait-audit.mjs` passes and renders all 36 movement frames; `player-walk-cycle.spec.cjs` observes all nine poses advancing forward during real left, right, up and down canvas walks. |
| BUG-047 | P2 | **Resolved 2026-09-19.** Side poses are scaled by subject height rather than width and share a 280x375 canvas, eliminating the 302–365 px size pump. | Chromium pixel audit reports 0 px subject-height spread across all 36 movement frames; the four-direction browser walk passes. |
| BUG-048 | P2 | **Resolved 2026-09-19.** The authored gait is a forward loop, never a ping-pong; frame 9 advances only to frame 1. | Unit phase-boundary coverage and the real-canvas four-direction test assert every observed transition is `n→n+1` or `9→1`. |
| BUG-049 | P2 | **Resolved 2026-09-19.** Walk phase advances from actual distance travelled against a 0.45-body-height step, so perspective-scaled movement and animation cadence scale together. | `player-gait.test.mjs` proves one body-relative step wraps identically at five drawn heights and preserves sub-frame distance; browser integration passes in every direction. |
| BUG-051 | P3 | **Resolved 2026-09-19.** Detached sheet debris is discarded before registration and both lateral directions derive from the cleaned left source plus the retained passing pose. | Pixel audit reports one y=371 subject baseline and 0 px baseline spread; the regenerated right-facing contact sheet shows every boot grounded correctly. |
| BUG-052 | P3 | **Resolved 2026-09-19.** The manifest now classifies the wired Section 2 frames as `player`, the legacy folder as `reference`, and accepts both runtime and legacy sprite-table paths without semantic collisions. Room calibration draws the wired idle. | Regenerated `asset-report.md` contains 40 wired player rows and no player budget breach; `player-frame-geometry.md` reports 10 frames per direction, all on 280x375 canvases; art reports render successfully. |
| BUG-039 | P3 | **Resolved 2026-09-19 by Leigh's acceptance.** The 40-frame painted player package has the approved finish, true lateral crossover, readable alternating front/back steps, stable 280x375 registration, contact shadows, and distance-driven forward playback. Leigh accepted the generated package as final without a further human paint-over. | All gait/pixel gates pass; `player-walk-cycle.spec.cjs` observes all nine poses in all four directions; Leigh supplied final human acceptance in this task. |
| BUG-053 | P2 | **Reported and resolved 2026-09-19.** A rejected no-go click left a stale hover cell/red cursor that could be reused as the target of later clicks. Click processing now derives the grid target from each actual pointer/touch event and clears failed movement/action state. | `click-to-walk.spec.cjs` reproduces the red-cursor/stale-hover state, submits a different valid click, and proves the target and movement recover. Navigation passes 9/9. |
| BUG-054 | P2 | **Reported and resolved 2026-09-19.** Player/NPC frames were allocated or decoded on demand, producing first-walk flicker. One shared cache now owns each URL, startup preloads and decodes all player/NPC movement frames, and renderers reuse those instances. | `image-cache.test.mjs` proves one decoded image per URL and renderer reuse; browser navigation proves every movement frame is cached before play. The unit suite passes 76/76. |

## Resolved in the foundation pass

| ID | Sev | Resolution |
| --- | --- | --- |
| BUG-025 | P1 | `npm start` referenced a missing `server.js`. A local Express static server and health endpoint now exist. Verified indirectly by the Playwright startup journey. |
| BUG-026 | P1 | There was no repeatable real-browser proof that the app opens and starts. The `startup` Playwright test now selects English, clicks New Game, and verifies the canvas/UI without local request or page errors. |
| BUG-027 | P1 | The first state-store adapter replaced the live player object on each property update, breaking click-to-walk code that retains the object during a frame. Player updates now preserve identity; the real-click navigation regression passes. |
| BUG-028 | P2 | Asynchronous background-image `onload` callbacks could let an older requested image overwrite a newer room. The URL is now selected synchronously after readiness; reduced-motion intro sequencing passes. |
| BUG-030 | P1 | During Section 3 verification, new-session reset cleared the stable default verb ID and made ordinary click-to-walk commands inert. Reset now restores `walkTo`; click movement and the full canonical open/locked navigation tests pass. |
